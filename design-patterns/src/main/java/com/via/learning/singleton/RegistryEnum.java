/**
 * 
 */
package com.via.learning.singleton;

/**
 * @author va.alberto
 * @description Ref. Google I/O 2k8 Joshua Bloch since Java 1.5 using enum we
 *              can create a singleton. It handles serialization using java's
 *              in-built mechanism and still ensure single instance
 */
public enum RegistryEnum {

	INSTANCE;

}
